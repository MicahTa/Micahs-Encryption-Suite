#TODO
'''
Enable SSL webview.start(ssl=True)?
Encrypt memory of sensitive data
Zero Out Memory After Use
Obfuscation
Enable Data Execution Prevention (DEP) to prevent execution of malicious code injected into data memory regions.
'''
#TODO
'''
On all of the actions you can take on the file exp page it will place a '/' in front of the destination and target
This will create a problem with Windows as it will try to acsses '/C:/'
So create a windows fix for every single one of those buttons
Both destination and target
'''


from api import initiateApp

if __name__ == "__main__":
    app = initiateApp()
